DATABASE_URL="postgresql://homekeep:homekeep@localhost:5432/homekeep"
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_FROM="HomeKeep <noreply@homekeep.app>"

BETTER_AUTH_SECRET=""        # openssl rand -base64 32
BETTER_AUTH_URL="http://localhost:5173"
PUBLIC_BETTER_AUTH_URL="http://localhost:5173"   # URL publique de l'app (browser → serveur)

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
PUBLIC_GOOGLE_ENABLED="false"   # passer à "true" quand les credentials sont renseignés
